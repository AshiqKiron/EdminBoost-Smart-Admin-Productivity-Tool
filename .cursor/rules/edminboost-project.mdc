---
description: Core conventions for the EdminBoost WordPress plugin
alwaysApply: true
---

# EdminBoost Project Rules

WordPress admin productivity plugin. PHP 7.4+, WordPress 6.0+, GPL-3.0.

See `edminboost-standards.mdc` (quality standards), `edminboost-compliance.mdc` (security/directory gate), `edminboost-features.mdc` (feature modules).

## Directory Layout

```
edminboost-smart-admin-roductivity-tool.php   # bootstrap + constants
includes/
  class-edminboost-plugin.php                 # hook orchestrator
  class-edminboost-admin.php                  # menus, settings UI, assets
  class-edminboost-settings.php               # options, defaults, sanitize
  class-edminboost-features.php               # feature registry
  class-edminboost-{activator,deactivator,i18n}.php
  features/
    class-edminboost-feature-base.php
    class-edminboost-{feature}.php
admin/
  css/  js/  partials/
languages/                                    # translation files (Domain Path)
uninstall.php
readme.txt
```

## Architecture

- Bootstrap defines constants, requires core classes, registers activation/deactivation hooks, and calls `edminboost_run_plugin()`.
- `EDMINBOOST_PLUGIN_SLUG` is the canonical plugin slug; `EDMINBOOST_TEXT_DOMAIN` is set from it.
- `EDMINBOOST_Plugin::run()` wires i18n, admin menu/settings/assets, and `EDMINBOOST_Features::register_hooks()` on `admin_init`.
- Settings: `edminboost_settings` via `EDMINBOOST_Settings` (group `edminboost_settings_group`). Global kill switch: `enabled`.
- Version tracker: `edminboost_version` (`EDMINBOOST_Settings::VERSION_OPTION`), seeded on activation.
- Features extend `EDMINBOOST_Feature_Base` in `includes/features/`, loaded by `EDMINBOOST_Features`.
- Admin UI: `EDMINBOOST_Admin` + partials in `admin/partials/` (feature fields in `edminboost-settings-features.php`).
- Assets enqueued only on plugin screens via `EDMINBOOST_Admin::is_plugin_screen()`.
- No custom tables, REST endpoints, cron, or blocks — options API only.
- No external requests or telemetry.

## Admin Pages

Use `EDMINBOOST_Admin::PAGE_SLUG` (equals `EDMINBOOST_PLUGIN_SLUG`) — never hardcode slug strings.

| Page | Slug | Settings API screen |
|------|------|---------------------|
| Dashboard | `PAGE_SLUG` | — |
| Settings | `PAGE_SLUG . '-settings'` | same suffix |

Capability: `EDMINBOOST_Settings::CAPABILITY` (`manage_options`).

## Naming

Project prefix: `edminboost` (10 characters). Use consistently in all identifiers.

| Kind | Pattern | Example |
|------|---------|---------|
| Constants | `EDMINBOOST_*` | `EDMINBOOST_VERSION` |
| Plugin slug / text domain | `edminboost-smart-admin-roductivity-tool` | `EDMINBOOST_PLUGIN_SLUG`, `EDMINBOOST_TEXT_DOMAIN` |
| Classes | `EDMINBOOST_{Name}` | `EDMINBOOST_Admin` |
| Functions | `edminboost_{action}` | `edminboost_run_plugin()` |
| Options | `edminboost_{key}` | `edminboost_settings` |
| Feature IDs | `snake_case` | `hide_admin_notices` |
| CSS/JS handles | `edminboost-{name}` | `edminboost-admin` |
| CSS classes | `edminboost-*` | `edminboost-wrap` |
| JS global (localized) | `edminboostData` | via `wp_localize_script()` |

`EDMINBOOST_TEXT_DOMAIN` must match `EDMINBOOST_PLUGIN_SLUG`.

## Extension Hooks

- `edminboost_feature_classes` — add/remove feature class names before instantiation
- `edminboost_features_loaded` — after features are registered
- `edminboost_settings_defaults`, `edminboost_settings`, `edminboost_sanitize_settings`
- `edminboost_is_feature_enabled` — custom feature enable logic

## Scope & Change Discipline

- Admin-only plugin — no frontend hooks unless explicitly requested.
- Minimize diff scope; match existing file structure and coding style.
- Add `index.php` silence files when creating new directories.
- Update `readme.txt` changelog for user-facing changes.

## Constants (use, don't redefine)

`EDMINBOOST_VERSION`, `EDMINBOOST_PLUGIN_FILE`, `EDMINBOOST_PLUGIN_DIR`, `EDMINBOOST_PLUGIN_URL`, `EDMINBOOST_PLUGIN_BASENAME`, `EDMINBOOST_PLUGIN_SLUG`, `EDMINBOOST_TEXT_DOMAIN`
